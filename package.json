{
  "name": "flashwise",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "闪学 Flashwise - AI flashcards, quizzes, and spaced repetition for Chinese learners.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "lint": "node scripts/lint.mjs",
    "typecheck": "node scripts/typecheck.mjs",
    "verify": "npm.cmd run lint && npm.cmd test && npm.cmd run typecheck && npm.cmd run build"
  }
}
